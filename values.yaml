hpa:
  cpuUtilization: '90'
  enabled: false
  maxReplicas: '3'
  memoryUtilization: 600Mi
  minReplicas: '1'
image:
  imagePullSecrets:
    - name: ghcr-secret
  pullPolicy: IfNotPresent
  repository: ghcr.io/fitnest-code/support-backend
  tag: development-73121f3
networkPolicy:
  allowedServices: []
  enabled: true
replicaCount: 1
resources:
  requests:
    cpu: 200m
    memory: 640Mi
  limits:
    cpu: 2000m
    memory: 1280Mi
service:
  grpcPort: 9090
  port: 8080
  type: ClusterIP
vaultEnv: development
database:
  hikariMaxPool: 30
  hikariMinIdle: 10
